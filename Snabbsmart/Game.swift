import Foundation



class Game {
    let questionCount: Int
    let token: String

    init(questionCount: Int, token: String) {
        self.questionCount = questionCount
        self.token = token
    }
}
